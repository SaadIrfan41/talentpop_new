import { Inria_Sans, Lato } from 'next/font/google'

export const titleFont = Inria_Sans({
  weight: ['400', '700'],

  subsets: ['latin'],
})

export const textFont = Lato({
  weight: ['300', '400', '700'],
  subsets: ['latin'],
})
